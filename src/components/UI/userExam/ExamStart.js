import React, { useState, useEffect } from "react";

const ExamStart = ({ examName }) => {
  //   const [examinfo, setExaminfo] = useState([]);
  //   console.log(examName);

  return (
    <div>
      {examName}
      <br />
    </div>
  );
};

export default ExamStart;
